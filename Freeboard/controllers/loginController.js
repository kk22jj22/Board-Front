/*
const authRequestModel = require("../model/request/auth")

class LoginController{

    login(email, passward){
        
        const body = new authRequestModel.Auth(email, passward)

        fetch("url", {
            body,
            headers:{
                
            }
        })
    }

}

module.exports = {
    LoginController
}
*/